package io.android.firebise;import android.content.Context;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.util.Log;import android.widget.Button;import android.widget.TextView;import com.google.firebase.database.DataSnapshot;import com.google.firebase.database.DatabaseError;import com.google.firebase.database.DatabaseReference;import com.google.firebase.database.FirebaseDatabase;import com.google.firebase.database.ValueEventListener;public class MainActivity extends AppCompatActivity {    private DatabaseReference f = FirebaseDatabase.getInstance().getReference();    private DatabaseReference usuario = f.child("usuario");   // private DatabaseReference produto = f.child("produto");   // private TextView t;    private Button btn;    //private Context c = null;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        //dados = (EditText) findViewById(R.id.edt);        btn = (Button) findViewById(R.id.btn);        //t = (TextView) findViewById(R.id.t);/*        Usuario u = new Usuario();        u.setNome("Rafael");        u.setSobreNome("Carvalho");        u.setIdade(31);        Produto p = new Produto();        //usuario.child("001").setValue(u);        p.setProduto(""+dados.getText().toString());        p.setCor("PRETO");        p.setFabricante("APPLE");        btn.setOnClickListener(new View.OnClickListener() {            int i =0;            @Override            public void onClick(View view) {                if(dados != null){                   String v = Integer.toString(i);                    produto.child(v).setValue(p);                    i ++;               }            }        });    */        usuario.addValueEventListener(new ValueEventListener() {            @Override            public void onDataChange(DataSnapshot dataSnapshot) {                Log.i("F", dataSnapshot.getValue().toString());            }            @Override            public void onCancelled(DatabaseError databaseError) {            }        });    }}